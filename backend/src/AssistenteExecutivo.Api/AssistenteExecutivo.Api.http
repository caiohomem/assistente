@AssistenteExecutivo.Api_HostAddress = http://localhost:5239

### Iniciar login (Keycloak UI)
GET {{AssistenteExecutivo.Api_HostAddress}}/auth/login?returnUrl=/

### Iniciar login com provedor (ex: google)
GET {{AssistenteExecutivo.Api_HostAddress}}/auth/login?provider=google&returnUrl=/

### Consultar sessão (retorna csrfToken + user se autenticado)
GET {{AssistenteExecutivo.Api_HostAddress}}/auth/session
Accept: application/json

### Logout (requer CSRF: envie cookie XSRF-TOKEN e header X-CSRF-TOKEN)
# 1) Chame /auth/session para receber o csrfToken e cookie XSRF-TOKEN.
# 2) Copie o valor de XSRF-TOKEN e cole em X-CSRF-TOKEN.
POST {{AssistenteExecutivo.Api_HostAddress}}/auth/logout
X-CSRF-TOKEN: {{csrfToken}}

